# Master 위성 배치 전략 정량 분석

## 1. 분석 목적

지상국(GS) 없이 궤도 상에서만 연합학습(FL)을 수행하는 **Orbital Data Center** 구조를 설계하기 위해,
집계(aggregation)를 담당하는 **Master 위성의 배치 방식**과 **최적 개수**를 정량적으로 결정한다.

### 비교 대상

| 구분 | 설명 |
|------|------|
| **Option A** | Worker와 같은 궤도면(570km)에 Master를 배치 |
| **Option B** | Worker와 다른 고도(600/620/650km)에 전용 Master를 배치 |

### 평가 지표

- **1차 전달 지연**: Worker가 학습된 모델을 담당 Master까지 ISL로 전달하는 시간
- **2차 집계 지연**: Master들 간에 글로벌 모델을 동기화하는 시간
- **총 End-to-End(E2E)**: Worker 학습 완료 → 글로벌 모델 완성까지의 총 소요 시간
- **Worker 손실률**: Master 전용 위성으로 인해 학습에 참여하지 못하는 위성 비율

---

## 2. 콘스텔레이션 구성

실제 시뮬레이션에 사용하는 Walker-Delta 콘스텔레이션 파라미터를 그대로 사용한다.

| 파라미터 | 값 | 비고 |
|----------|-----|------|
| 총 위성 수 | 238 | 17 planes × 14 sats/plane |
| 궤도 고도 | 570 km | 원형 궤도 (e=0) |
| 경사각 (Plane 0) | 80° | Polar plane (1개) |
| 경사각 (Plane 1~16) | 70° | 일반 plane (16개) |
| Walker 위상계수 F | 1 | 인접 면 간 Mean Anomaly 오프셋 = 360°/(17×14) ≈ 1.51° |
| 궤도 주기 | 95.92 min | a = 6,941 km 기준 |
| Epoch | 2026-02-18 00:00 UTC | |

### Walker 위상계수 F의 의미

F는 인접 궤도면 간 위성의 위상 오프셋을 결정하는 파라미터이다.
각 위성의 Mean Anomaly는 다음과 같이 계산된다:

```
M(p, s) = s × (360°/14) + p × (F × 360°) / (17 × 14)
```

- `p`: 궤도면 번호 (0~16)
- `s`: 면 내 위성 인덱스 (0~13). `s × (360°/14)`로 같은 면 내 14개 위성을 25.7° 간격으로 균등 배치
- `F=0`이면 모든 면의 위성이 같은 위상에서 시작 (정렬 배치)
- `F=1`이면 인접 면마다 약 1.51°씩 위상이 어긋남 → 지표면 커버리지 균등 분산

본 설계에서 `F=1`을 사용하므로, 인접 면의 위성들은 약간 엇갈린 위치에 분포한다.

---

## 3. ISL 통신 모델

### 설계 전제

본 연구에서는 **인접 궤도면 간 ISL은 항상 가능**하다고 가정한다.
이는 Starlink v2 등 현세대 LEO 위성이 레이저 ISL을 통해
인접 면 위성과 상시 연결을 유지하는 구조를 반영한 것이다.

따라서 ISL 통신 가능 여부는 거리 임계값으로 판단하지 않으며,
거리는 오직 **전파 지연(propagation delay)** 계산에만 사용한다.

### ISL 1홉 시간 구성

ISL 1홉 시간은 세 가지 요소의 합으로 구성된다:

```
t_hop = t_transfer + t_overhead + t_propagation
```

| 구성 요소 | 산출 근거 | 값 |
|-----------|-----------|-----|
| **모델 전송 시간** (t_transfer) | ResNet-9 ≈ 26MB, ISL 대역폭 100Mbps, `26×8/100` | **2.08초** |
| **핸드셰이크+확인응답** (t_overhead) | 링크 설정, ACK 등 프로토콜 오버헤드 | **5.00초** |
| **전파 지연** (t_propagation) | 위성 간 거리 / 광속 | 아래 참조 |

> 전파 지연은 거리에 비례하지만, 모델 전송(2.08초)과 오버헤드(5초)가 지배적이므로
> intra/inter-plane 간 1홉 시간 차이는 매우 작다.

### 전파 지연(propagation delay) 계산 근거

전파 지연은 전자기파가 두 위성 사이의 거리를 이동하는 데 걸리는 시간이다.
ISL은 레이저 또는 RF 링크이며, 진공(우주 공간)에서의 전파 속도는 광속과 동일하다.

```
t_propagation = d / c

여기서:
  d = 두 위성 간 유클리드 거리 (km)
  c = 광속 = 299,792.458 km/s
```

예시 계산:
- Intra-plane (d = 3,089 km): 3,089 / 299,792 = 0.01030초 = **10.3 ms**
- Inter-plane (d = 1,096 km): 1,096 / 299,792 = 0.00366초 = **3.7 ms**

전파 지연이 수~수십 ms 수준인 반면, 모델 전송 시간(2.08초)과 오버헤드(5초)의 합이 7.08초이므로,
전파 지연이 전체 홉 시간에 미치는 영향은 0.05~0.15% 수준에 불과하다.
이로 인해 intra-plane 홉(7.09초)과 inter-plane 홉(7.08초)의 시간이 사실상 동일해진다.

---

## 4. SGP4 실측 결과: 위성 간 거리

24시간 동안 10분 간격(144개 시점)으로 SGP4 전파하여 실제 ECI 좌표 기반 거리를 측정했다.

### 4.1 Intra-plane 거리 (같은 면 내 인접 위성)

> 측정 대상: Plane 1 (경사각 70°)의 14개 위성, 인접 위성 쌍

#### 측정 방법

1. Plane 1의 14개 위성(sat_id 14~27)을 SGP4로 각 시점에 전파하여 ECI(Earth-Centered Inertial) 좌표 (x, y, z)를 구한다
2. 인접 위성 쌍의 유클리드 거리를 계산한다: `d = √((x₁-x₂)² + (y₁-y₂)² + (z₁-z₂)²)`
3. 14개 위성은 ring topology이므로, 각 시점에서 14개 인접 쌍 (sat14↔sat15, sat15↔sat16, ..., sat27↔sat14)의 거리를 측정한다
4. 144개 시점 × 14쌍 = 2,016개 거리 샘플에 대해 통계를 낸다

#### 인접 위성 거리의 이론적 근거

같은 궤도면의 14개 위성은 동일한 원형 궤도(반경 a = 6,941km) 위에 균등 배치된다.
인접 위성 간 중심각 θ = 360°/14 ≈ 25.71°이고, 현(chord) 거리는 다음과 같다:

```
d = 2 × a × sin(θ/2) = 2 × 6,941 × sin(12.86°) = 2 × 6,941 × 0.2225 ≈ 3,089 km
```

원형 궤도에서 위성 간 상대 속도가 0이므로 거리는 시간에 따라 거의 변하지 않는다.
실측 결과(min=3,085km, max=3,092km)가 이 이론값과 일치하며,
미세한 변동은 SGP4의 J2 섭동(지구 편평도) 보정에 의한 것이다.

#### 측정 결과

| 통계량 | 값 |
|--------|-----|
| 평균 | 3,089.5 km |
| 최소 | 3,085.3 km |
| 최대 | 3,091.7 km |

- 원형 궤도이므로 인접 위성 간 거리는 거의 일정하다
- 14개 위성이 360°를 균등 분할 → 인접 위성 간 각도 = 360°/14 ≈ 25.7°
- 전파 지연: 3,089 km / 299,792 km/s ≈ **10.3 ms**

**Intra-plane 1홉 시간: 2.08 + 5.00 + 0.010 = 7.09초**

### 4.2 Inter-plane 거리 (인접 궤도면)

> 측정 대상: Plane 1 ↔ Plane 2 (둘 다 경사각 70°), 각 시점의 면 간 최소 거리

#### 측정 방법

1. Plane 1의 14개 위성과 Plane 2의 14개 위성을 각 시점에 SGP4로 전파한다
2. 14×14 = 196개의 모든 위성 쌍에 대해 ECI 유클리드 거리를 계산한다
3. 각 시점에서 **196쌍 중 최소 거리**(= 인접 면에서 ISL 상대로 가장 가까운 위성 쌍)와
   **196쌍의 평균 거리**를 기록한다
4. 144개 시점에 대해 통계를 낸다

"최소 거리"를 사용하는 이유는, 실제 inter-plane ISL에서는 가장 가까운 위성 쌍이
통신 상대가 되기 때문이다.

#### 인접 면 간 거리가 같은 면 인접 위성보다 짧은 이유

같은 면 내 인접 위성 간 각도는 360°/14 ≈ 25.7°이다.
반면 인접 궤도면의 RAAN 간격은 360°/17 ≈ 21.18°이다.

같은 면 내 인접 위성은 **같은 궤도 원 위**에 있으므로 현(chord) 거리 = 2a×sin(25.7°/2) ≈ 3,089km이다.
인접 면의 가장 가까운 위성은 **다른 궤도 원 위**이지만, 두 면이 비슷한 위도를 통과할 때
RAAN 차이(21.18°)에 cos(위도)를 곱한 실효 각도가 25.7°보다 작아지므로
더 짧은 거리(~1,096km)가 된다.

#### 측정 결과

| 통계량 | 최소 거리 (14×14 쌍 중 최소) | 평균 거리 (전체 쌍 평균) |
|--------|------|------|
| 평균 | 1,096.3 km | 8,964.0 km |
| 최소 | 1,052.2 km | - |
| 최대 | 1,178.5 km | - |

- RAAN 간격 = 360°/17 ≈ 21.18°
- **인접 면의 가장 가까운 위성 쌍 거리(~1,096km)가 같은 면 인접 위성 거리(~3,089km)보다 짧다**
- 최소 거리의 변동(1,052~1,179km)은 Walker F=1에 의한 위상 오프셋과 J2 섭동의 결합 효과
- 전파 지연: 1,096 km / 299,792 km/s ≈ **3.7 ms**

**Inter-plane 1홉 시간: 2.08 + 5.00 + 0.004 = 7.08초**

### 4.3 Polar Plane (Plane 0, 80°) ↔ Plane 1 (70°) 거리

| 통계량 | 최소 거리 |
|--------|------|
| 평균 | 976.3 km |
| 최소 | 840.5 km |
| 최대 | 1,128.2 km |

경사각 차이(10°)가 있지만 ISL 가능 범위 내이며, 변동폭이 일반 면 간보다 크다.

### 4.4 핵심 관찰: Intra ≈ Inter 홉 시간

| | 위성 간 거리 | 전파 지연 | **1홉 총 시간** |
|--|--|--|--|
| Intra-plane (같은 면) | 3,089 km | 10.3 ms | **7.09초** |
| Inter-plane (인접 면) | 1,096 km | 3.7 ms | **7.08초** |

전파 지연은 수 ms 수준으로, 모델 전송(2.08초) + 오버헤드(5초) 대비 무시할 수 있다.
따라서 **intra/inter-plane 홉 시간이 사실상 동일(~7.1초)**하며,
전달 시간은 순수하게 **홉 수에 비례**한다.

---

## 5. Option A: 같은 궤도면 Master (동일 고도 570km)

### 5.1 구조

- 238개 위성 중 N개를 Master로 지정, 나머지는 Worker
- Master는 17개 궤도면에 균등 배치 (예: 3개면 → Plane 0, 6, 12)
- Worker → Master 전달: intra-plane + inter-plane ISL 홉
- **상시 연결**: 같은 면 내 ISL은 항상 가능, 인접 면도 항상 가능

### 5.2 전달 시간 모델

Worker → 담당 Master까지의 전달 시간은 두 구간의 합이다:

```
t_delivery = (inter-plane 홉 수) × t_hop_inter + (intra-plane 홉 수) × t_hop_intra
```

**Inter-plane 홉 수**: Worker가 속한 면에서 가장 가까운 Master가 있는 면까지의 거리.
N개의 Master가 균등 배치되면, 최대 inter-plane 홉 수 = ceil(17 / (2×N)).

**Intra-plane 홉 수**: Master가 있는 면에 도착한 뒤, 면 내에서 Master 위성까지의 홉 수.
14개 위성이 ring topology를 형성하고, 그 중 1개가 Master이다.
나머지 13개 Worker는 시계/반시계 양방향 중 짧은 쪽으로 Master에 전달한다.

```
      Master(0)
     /         \
  (13)         (1)      각 Worker의 최단 홉 수:
   |             |        sat1=1, sat2=2, ..., sat7=7, sat8=6, ..., sat13=1
  (12)         (2)
   |             |      최단 홉 목록: {1, 2, 3, 4, 5, 6, 7, 6, 5, 4, 3, 2, 1}
  (11)         (3)
   |             |      합계 = 49
  (10)         (4)      평균 = 49 / 13 ≈ 3.77홉
    \          /
     (9)  (5)
      (8)(6)
       (7)
```

코드에서는 이를 `N/4 = 14/4 = 3.5`로 근사한다.
정확한 값은 3.77홉이며, 이 근사에 의해 실제 전달 시간은 약 1.9초(0.27홉 × 7.09초) 과소추정된다.
본 분석에서는 상대 비교가 목적이므로 모든 Master 수에 동일하게 적용되어 결론에는 영향을 주지 않는다.

### 5.3 분석 항목 정의 및 계산 방법

각 항목의 정의와 함께 실제 코드에서 어떻게 계산되는지를 상세히 설명한다.

#### avg 전달 (평균 전달 시간)

**정의**: 전체 Worker가 학습 완료 후 담당 Master까지 모델을 ISL로 전달하는 시간의 전체 평균.

**계산 방법**:
1. N개의 Master를 17개 궤도면에 균등 배치한다. 예: Master 3개 → Plane 0, 6, 12에 배치
   - 배치 공식: `master_planes[i] = int(i × 17 / N)` (i = 0, 1, ..., N-1)
2. 238개 위성 각각에 대해 1차 전달 시간 `t_delivery`를 계산한다:
   - 해당 위성의 궤도면에서 가장 가까운 Master 면까지의 inter-plane 홉 수를 구한다
   - 17개 면이 원형 배치이므로, 시계/반시계 두 방향 중 짧은 쪽을 선택:
     `min_inter = min(|src - master|, 17 - |src - master|)` (각 Master에 대해)
   - Master가 있는 면 내에서 Master 위성까지의 intra-plane 평균 홉 수 ≈ 3.5홉 (정확값 3.77홉, 5.2절 참조)
   - `t_delivery = min_inter × 7.08초 + 3.5 × 7.09초`
3. Master 위성 자체는 Worker가 아니므로 제외 (N개 제외)
4. 나머지 (238 - N)개 Worker의 `t_delivery`를 평균낸다

예시 (Master 3개, Plane 0/6/12):
- Plane 0의 Worker: inter=0홉, t = 0×7.08 + 3.5×7.09 = 24.8초
- Plane 3의 Worker: inter=3홉(Plane 3→0), t = 3×7.08 + 3.5×7.09 = 46.1초
- Plane 9의 Worker: inter=3홉(Plane 9→6 또는 9→12), t = 3×7.08 + 3.5×7.09 = 46.1초
- 전체 Worker의 평균 → **avg전달 = 34.9초**

#### p95 전달 (95번째 백분위수 전달 시간)

**정의**: 전체 Worker 중 95%가 이 시간 이내에 Master에 도달한다. 소수의 최악 사례를 제외한 실질적 상한선.

**계산 방법**: 위에서 계산한 (238-N)개의 `t_delivery` 값을 오름차순 정렬하여 95번째 백분위수를 취한다.
numpy의 `np.percentile(delivery_times, 95)`를 사용한다.

#### max 전달 (최대 전달 시간)

**정의**: 콘스텔레이션에서 Master로부터 가장 멀리 위치한 Worker의 전달 시간. 최악의 경우.

**계산 방법**: (238-N)개의 `t_delivery` 중 최댓값.
이는 Master 배치에서 가장 먼 궤도면에 있는 Worker가 경험하는 시간이다.

#### max 홉 (최대 inter-plane 홉 수)

**정의**: Worker → 담당 Master까지의 면 간 이동에 필요한 최대 홉 수 (intra-plane 홉 제외).

**계산 방법**: `ceil(17 / (2 × N))`.
17개 면에 N개 Master를 균등 배치하면, 가장 먼 Worker는 Master로부터 최대 `17/(2N)` 면 떨어져 있다.

예: Master 3개 → `ceil(17/6)` = 3홉, Master 5개 → `ceil(17/10)` = 2홉

#### 2차 홉 (Master 간 동기화 라운드 수)

**정의**: 모든 Master가 서로의 집계 결과를 교환하여 동일한 글로벌 모델을 갖기 위해
필요한 Ring topology 상의 최대 라운드 수.

**중요: Master 간 직접 ISL은 불가능하다.**
본 설계에서 ISL은 **인접 궤도면 간에만 상시 가능**하다고 가정하므로,
예를 들어 Plane 0의 Master와 Plane 6의 Master는 6면 떨어져 있어 직접 통신할 수 없다.
따라서 Master 간 동기화는 **중간 궤도면의 Worker 위성을 경유하는 릴레이**로 이루어진다.

```
Master0(P0) ─────────────────────────── Master1(P6)
             직접 ISL 불가 (6면 간격)

실제 경로:
Master0(P0) → P1 → P2 → P3 → P4 → P5 → Master1(P6)
              홉1   홉2   홉3   홉4   홉5   홉6
              (각 홉은 인접 면 ISL 릴레이)
```

**계산 방법**: `ceil(N / 2)`.
N개의 Master가 ring을 형성하면, 양방향 동시 전파 시 가장 먼 Master까지 `N/2` 라운드가 필요하다.
각 라운드에서 인접 Master 방향으로 1단계씩 전파된다.

예: Master 3개 → `ceil(3/2)` = 2라운드, Master 5개 → `ceil(5/2)` = 3라운드

#### 2차 집계 (Master 간 동기화 시간)

**정의**: 1차 집계 후 모든 Master가 동일한 글로벌 모델을 갖기까지의 소요 시간.

**계산 방법**: `ceil(N/2) × (17/N) × t_hop_inter`.
- `ceil(N/2)`: 2차 라운드 수 (ring 양방향 전파)
- `17/N`: Master 간 면 간격 = **1라운드당 경유해야 하는 inter-plane 홉 수**
- `t_hop_inter`: inter-plane 1홉 시간 (7.08초)

**1라운드 = Master 간 간격만큼의 인접 면 릴레이 홉**이다.
Master 간 직접 통신이 아니라 중간 면을 모두 경유해야 하므로, Master 간격이 넓을수록 1라운드에 걸리는 시간이 길어진다.

예 (Master 3개, Plane 0/6/12):
```
1라운드당 홉 수 = 17/3 ≈ 5.67홉 (P0→P1→...→P6 경유)
총 라운드 = ceil(3/2) = 2라운드
2차 집계 시간 = 2 × 5.67 × 7.08초 = 80.3초
```

예 (Master 17개, 면마다 1개):
```
1라운드당 홉 수 = 17/17 = 1홉 (바로 인접 면이 다음 Master)
총 라운드 = ceil(17/2) = 9라운드
2차 집계 시간 = 9 × 1 × 7.08초 = 63.8초
```

Master가 1개일 때는 동기화 대상이 없으므로 0초이다.

#### 총 E2E (End-to-End 시간)

**정의**: Worker 학습 완료 시점부터 글로벌 모델이 완성되기까지의 총 소요 시간.

**계산 방법**: `avg전달 + 2차집계`.
1차 전달(Worker→Master)과 2차 집계(Master 간 동기화)가 순차적으로 발생하므로 합산한다.

```
[Worker 학습완료] ──avg전달──→ [Master 1차 집계] ──2차집계──→ [글로벌 모델 완성]
                                                             
|←──────────────── 총 E2E ────────────────────→|
```

#### 오버헤드 (Worker 손실률)

**정의**: Master 전용 위성으로 인해 학습에 참여하지 못하는 위성의 비율.

**계산 방법**: `N / 238 × 100%`.
Master는 집계만 수행하고 로컬 학습을 하지 않으므로, N개의 Master는 Worker에서 제외된다.

예: Master 3개 → 3/238 = 1.3%, Master 17개 → 17/238 = 7.1%

### 5.4 결과 테이블

```
hop_intra = 7.09초, hop_inter = 7.08초

Masters  Workers  avg전달  p95전달  max전달  max홉  2차홉  2차집계  총E2E   오버헤드
──────── ─────── ─────── ─────── ─────── ────── ────── ─────── ─────── ────────
   1       237     54.9s   81.5s   81.5s    9      0     0.0s    54.9s   0.4%
   2       236     40.0s   53.2s   53.2s    5      1    60.2s   100.2s   0.8%
   3       235     34.9s   46.1s   46.1s    3      2    80.3s   115.2s   1.3%
   4       234     32.4s   39.0s   39.0s    3      2    60.2s    92.7s   1.7%
   5       233     30.8s   39.0s   39.0s    2      3    72.3s   103.0s   2.1%
   6       232     29.5s   31.9s   31.9s    2      3    60.2s    89.7s   2.5%
   9       229     28.3s   31.9s   31.9s    1      5    66.9s    95.2s   3.8%
  17       221     24.8s   24.8s   24.8s    1      9    63.8s    88.6s   7.1%
```

### 5.5 결과 해석

**1차 전달 시간 (avg전달)**:
- Master 1개: 54.9초 → Master 3개: 34.9초 (20초 감소, -36%)
- Master 3개 → 5개: 30.8초 (4.1초 감소, -12%)
- Master 5개 이상에서는 개선폭이 급격히 줄어듦

**2차 집계 시간**:
- Master 1개: 2차 집계 불필요 (0초)
- Master 3개: 2홉 × ~40초 = 80.3초 추가
- Master 17개: 9홉이 필요하지만 간격이 1면이므로 63.8초

**총 E2E에서의 트레이드오프**:
1차 전달 시간은 Master가 많을수록 줄어들지만, 2차 집계가 추가되면서 총 E2E는 단순 감소하지 않는다.

#### 단일 장애점(SPOF: Single Point of Failure)이란

SPOF란 **시스템 전체의 동작이 하나의 구성 요소에 의존**하여,
해당 구성 요소가 고장나면 시스템 전체가 멈추는 구조적 취약점을 말한다.

Master 위성이 1개일 때:
- 해당 Master가 **하드웨어 고장, 방사선 손상, 통신 장애, 궤도 이탈** 등으로 기능을 상실하면
  전체 238개 위성의 연합학습이 즉시 중단된다
- 복구 수단이 없다 (궤도 상에서 교체 불가)
- 237개 Worker가 모두 이 1개 Master에 모델을 전달하므로 **통신 병목**도 발생 가능

Master가 3개 이상이면:
- 1개 Master가 고장나도 나머지 2개가 계속 동작 → **부분 장애 허용(fault tolerance)**
- 고장난 Master의 담당 Worker는 인접 Master로 재할당 가능
- 각 Master의 통신 부하가 1/3로 분산

#### Master 17개가 E2E 최소인데 왜 4~6개를 권장하는가

결과 테이블에서 Master 17개의 총 E2E(88.6초)가 Master 4개(92.7초)보다 낮다.
그럼에도 17개를 권장하지 않는 이유는 **E2E 이외의 비용이 크기 때문**이다:

**(1) Worker 손실 7.1% (17개 = 학습 참여 위성 221개)**

Master는 집계 전용이므로 로컬 학습을 수행하지 않는다.
Master 17개는 전체 238개 중 17개(7.1%)가 학습에서 빠진다는 의미이다.
연합학습에서 참여 클라이언트 수가 줄면 모델 정확도, 수렴 속도, 데이터 다양성에 직접적 영향을 준다.
반면 Master 4개는 1.7%만 손실되어 영향이 최소화된다.

| Masters | Worker 수 | 손실 | 비교 |
|---------|-----------|------|------|
| 4 | 234 | 1.7% | -4개 Worker |
| 6 | 232 | 2.5% | -6개 Worker |
| 17 | **221** | **7.1%** | **-17개 Worker** |

**(2) 2차 집계의 복잡도와 실질적 비용**

2차 집계 홉 수:
- Master 4개: 2홉 (단순)
- Master 17개: **9홉** (ring의 절반을 순회해야 함)

홉 수 9는 이론적 시간(63.8초)만의 문제가 아니다:
- 각 홉에서 **모델 병합(aggregation) 연산**이 추가로 발생할 수 있음
- 9단계의 순차 전파에서 **1개 Master의 지연이 전체 파이프라인을 차단**
- Ring 동기화의 복잡도가 증가하여 구현 및 디버깅 난이도가 상승

**(3) E2E 차이가 미미함 (88.6초 vs 89.7초 = 1.1초 차이)**

Master 17개(88.6초)와 6개(89.7초)의 E2E 차이는 불과 1.1초이다.
이 1.1초의 이득을 위해 Worker 11개(4.6%)를 추가 손실하고
2차 집계 홉을 9로 늘리는 것은 비용 대비 효과가 없다.

**(4) 종합 판단 기준**

| 기준 | Master 4개 | Master 6개 | Master 17개 |
|------|-----------|-----------|------------|
| 총 E2E | 92.7초 | 89.7초 | 88.6초 |
| Worker 손실 | 1.7% | 2.5% | **7.1%** |
| 2차 집계 홉 | 2 | 3 | **9** |
| 구현 복잡도 | 낮음 | 낮음 | **높음** |
| Fault tolerance | 3개 중복 | 5개 중복 | 16개 중복 (과잉) |

**4~6개가 최적인 이유**: E2E가 17개 대비 3~4초만 길면서,
Worker 손실을 1/3~1/4로 줄이고, 2차 집계 복잡도를 크게 낮춘다.
즉, **E2E의 소폭 증가를 감수하고 시스템 전체 효율을 극대화**하는 균형점이다.

---

## 6. Option B: 다른 고도 Master (전용 궤도)

### 6.1 구조

- Worker 콘스텔레이션(570km)과 **다른 고도**(600/620/650km)에 Master 전용 위성을 배치
- Master와 Worker가 **다른 궤도 주기**를 가지므로 상대 drift 발생
- 같은 궤도면(같은 RAAN)에 배치하여 면 내 ISL로 통신

### 6.2 궤도 주기 비교

| 고도 | 반경 (a) | 궤도 주기 | Worker 대비 주기 차이 |
|------|----------|----------|----------------------|
| 570 km (Worker) | 6,941 km | 95.92 min | - |
| 600 km | 6,971 km | 96.54 min | +37.35 sec/orbit |
| 620 km | 6,991 km | 96.95 min | +62.30 sec/orbit |
| 650 km | 7,021 km | 97.58 min | +99.78 sec/orbit |

### 6.3 SGP4 실측: Master ↔ Worker 거리

#### 측정 방법

다른 고도 Master가 Worker와 실제로 어떤 거리 관계를 갖는지 SGP4 전파로 측정한다.

**Master 위성 생성**:
1. Master 1개를 Plane 1과 동일한 RAAN(21.18°)에 배치하되, 고도만 변경한다
   - Kepler 요소: (R_EARTH + master_alt, e=0, i=70°, RAAN=21.18°, w=0°, M=0°)
   - 이 요소로 TLE를 생성하고 Satrec 객체를 만든다
2. Worker 콘스텔레이션의 Plane 1 위성 14개(sat_id 14~27)와 비교한다

**"면 전체와의 최소 거리" 측정**:
1. 24시간 동안 **1분 간격**(1,440개 시점)으로 Master와 Plane 1의 14개 Worker를 각각 SGP4 전파한다
2. 각 시점 t에서:
   - Master의 ECI 좌표 `r_master(t)`를 구한다
   - Plane 1의 14개 Worker 각각의 ECI 좌표 `r_worker_j(t)`를 구한다 (j = 0~13)
   - 14개의 거리 `d_j(t) = ||r_master(t) - r_worker_j(t)||`를 계산한다
   - 이 중 **최솟값** `d_min(t) = min(d_0, d_1, ..., d_13)`을 기록한다
3. 1,440개 시점의 `d_min(t)`에 대해 평균/최소/최대 통계를 낸다

이 `d_min(t)`는 "**해당 시점에 Master와 가장 가까운 Worker까지의 거리**"를 의미한다.
ISL 가능 여부는 이 값이 통신 범위 내인지로 판단한다.

**"특정 Worker 1개와의 거리" 측정** (600km 고도에서 추가 수행):
1. Master와 Plane 1의 첫 번째 Worker(sat_id 14) 사이의 거리만 측정한다
2. 이는 **특정 Worker 입장에서 Master에 모델을 전달하려면 얼마나 대기해야 하는지**를 보여준다

#### 600km (Δh = 30km)

**면 전체와의 최소 거리** (= 14개 Worker 중 가장 가까운 Worker까지의 거리):

| 통계량 | 값 |
|--------|-----|
| 평균 | 769 km |
| 최소 | 30 km |
| 최대 | 1,556 km |

- 최대 거리 1,556km: Plane 전체 14개 Worker 중 가장 가까운 것이 1,556km이라는 의미
- **24시간 내내 Plane 1의 "어떤" Worker와는 ISL이 가능**할 수 있으나,
  이는 해당 시점마다 다른 Worker가 가장 가까워지는 것일 뿐이다

**특정 Worker 1개(Plane 1, Sat 0)와의 거리**:

| 통계량 | 값 |
|--------|-----|
| 평균 | 3,142 km |
| 최소 | 30 km |
| 최대 | 5,846 km |

이 결과가 Option B의 핵심 문제를 보여준다:
- Master와 특정 Worker의 거리가 30km~5,846km까지 극심하게 변동
- 500km 이내 근접: 24시간 중 약 4~5회, 회당 약 6~7분
- Worker가 학습을 완료한 시점에 Master가 근처에 없다면 **다음 근접까지 수 시간 대기**해야 한다
- 이는 GS 기반 시스템에서 "위성이 GS 상공을 지날 때까지 대기"하는 것과 동일한 구조적 병목

#### 620km (Δh = 50km)

| 통계량 | 값 |
|--------|-----|
| Plane 1과의 최소 거리 — 평균 | 761 km |
| Plane 1과의 최소 거리 — 최소 | 50 km |
| Plane 1과의 최소 거리 — 최대 | 1,561 km |

drift가 더 빠르므로(62초/orbit) 특정 Worker와의 근접 빈도는 높아지지만,
여전히 간헐적 접촉 구조이다.

#### 650km (Δh = 80km)

| 통계량 | 값 |
|--------|-----|
| Plane 1과의 최소 거리 — 평균 | 823 km |
| Plane 1과의 최소 거리 — 최소 | 80 km |
| Plane 1과의 최소 거리 — 최대 | 1,565 km |

drift가 가장 빠르지만(100초/orbit), 고도 차이로 인해 최소 접근 거리가 80km로 증가.
아무리 가까이 지나가도 고도 차이 80km 이하로는 접근할 수 없다.

### 6.4 핵심 문제: GS 병목과 동일한 구조

다른 고도 Master의 근본적 문제는 **특정 Worker와의 1:1 통신이 간헐적**이라는 점이다.

```
같은 고도 Master:  Worker ←──상시 ISL──→ Master  (항상 전달 가능)

다른 고도 Master:  Worker ←─ 간헐 접촉 ─→ Master  (대기 필요)
                   ↑ 주기 차이로 drift     ↑ 근접 시에만 통신
```

이 구조는 현재 GS 기반 시스템에서 **위성이 서울 GS 상공을 지날 때만 통신 가능**한 것과
본질적으로 동일한 병목이다. Orbital Data Center의 핵심 목표가 GS 병목 제거인데,
다른 고도 Master는 그 병목을 궤도 상으로 옮긴 것에 불과하다.

---

## 7. 종합 비교

### Option A vs Option B

| 비교 항목 | Option A (같은 고도) | Option B (다른 고도) |
|-----------|---------------------|---------------------|
| Worker→Master 연결성 | **상시** (ISL always-on) | 간헐적 (drift 기반) |
| 평균 전달 시간 | **25~55초** (홉 수 비례) | 수분~수시간 (접촉 대기) |
| 전달 지연 결정 요인 | ISL 홉 수 (확정적) | drift 속도 + 접촉 타이밍 (확률적) |
| 추가 발사 비용 | **불필요** (기존 위성 활용) | 필요 (별도 궤도) |
| 장기 운용 | 궤도 유지 동일 | 고도 차이로 인한 drift 관리 필요 |
| GS 병목 해소 여부 | **완전 해소** | 병목을 궤도로 이전 (미해소) |

**결론: Option A (같은 궤도면, 동일 고도) 선택**

---

## 8. Master 수 최적화 (Option A 기준)

### 8.1 Marginal Gain 분석

Master를 1개씩 추가할 때 avg 전달 시간이 얼마나 줄어드는지 분석한다.

```
전환     avg전달 개선  E2E 변화   추가 Master  Master당 이득
──────── ──────────── ────────── ─────────── ─────────────
1 →  2   +15.0s       -45.2s      1개         15.0s/개
2 →  3    +5.0s       -15.1s      1개          5.0s/개    ← 효율 급감 시작
3 →  4    +2.5s       +22.6s      1개          2.5s/개
4 →  5    +1.7s       -10.4s      1개          1.7s/개
5 →  6    +1.3s       +13.3s      1개          1.3s/개
6 →  9    +1.2s        -5.5s      3개          0.4s/개    ← 사실상 무의미
9 → 17    +3.5s        +6.6s      8개          0.4s/개
```

### 8.2 해석

1. **1→2 전환이 가장 큰 이득** (Master당 15.0초 개선)
   - 단일 Master의 병목을 제거하는 효과
2. **2→3 전환까지가 의미 있는 구간** (Master당 5.0초 개선)
   - 3개 이후 marginal gain이 급격히 감소
3. **4개 이상은 diminishing return**
   - Master당 2.5초 이하 개선, 2차 집계 비용이 상쇄

### 8.3 E2E 관점의 최적점

E2E = avg전달 + 2차집계이므로, 1차 전달 감소분보다 2차 집계 증가분이 큰 구간이 있다:

| Masters | avg전달 | 2차집계 | **총 E2E** | 비고 |
|---------|---------|---------|-----------|------|
| 1 | 54.9s | 0.0s | **54.9s** | SPOF, 병목 위험 |
| 3 | 34.9s | 80.3s | 115.2s | |
| 4 | 32.4s | 60.2s | **92.7s** | 다중 Master 중 최적 |
| 6 | 29.5s | 60.2s | **89.7s** | E2E 최소 |
| 17 | 24.8s | 63.8s | 88.6s | Worker 7.1% 손실 |

- **SPOF 허용 시**: Master 1개 (E2E = 54.9초)
- **안정성 필요 시**: Master 4개 (E2E = 92.7초, Worker 손실 1.7%)
- **E2E 최소화**: Master 6개 (E2E = 89.7초, Worker 손실 2.5%)

### 8.4 2-tier 계층 구조

Master가 2개 이상이면 글로벌 모델 동기화를 위한 **2차 집계**가 필요하다.

```
           ┌───────┐   ┌───────┐   ┌───────┐
           │Master0│───│Master1│───│Master2│     ← Tier 2: Master 간 Ring 동기화
           │(P0)   │   │(P6)   │   │(P12)  │
           └───┬───┘   └───┬───┘   └───┬───┘
               │           │           │
          ┌────┴────┐ ┌────┴────┐ ┌────┴────┐
          │Workers  │ │Workers  │ │Workers  │   ← Tier 1: Worker → Master
          │P0~P5    │ │P6~P11  │ │P12~P16 │      (intra + inter-plane ISL)
          └─────────┘ └─────────┘ └─────────┘
```

**Tier 1 (Worker → Master)**: 각 Worker는 가장 가까운 Master에 모델 전달.
최대 3개 inter-plane 홉 + 평균 3.77개 intra-plane 홉 (5.2절 참조).

**Tier 2 (Master 간 동기화)**: Master 간 직접 ISL은 불가능하다 (인접 면만 ISL 가능).
따라서 중간 궤도면의 Worker 위성을 릴레이로 경유하여 동기화한다.

```
Tier 2 실제 경로 (Master0 → Master1):

Master0(P0) → [P1] → [P2] → [P3] → [P4] → [P5] → Master1(P6)
               ↑      ↑      ↑      ↑      ↑
          Worker 위성들이 ISL 릴레이 역할 수행
          (각 면의 가장 가까운 Worker가 중계)
```

Master 3개 기준: 1라운드당 약 5.67홉 릴레이 × 2라운드 = 약 80초 소요.

---

## 9. 권장 설계

| 항목 | 권장값 | 근거 |
|------|--------|------|
| **Master 배치** | 같은 궤도면 (570km) | 상시 ISL 연결, GS 병목 완전 해소 |
| **Master 수** | 3~4개 | Marginal gain 최대 구간, Worker 손실 < 2% |
| **Master 배치 면** | 균등 분산 (예: P0, P6, P12) | 최대 inter-plane 홉 = 3 |
| **집계 구조** | 2-tier 계층 | Tier 1: Worker→Master, Tier 2: Master 간 Ring |
| **Worker 손실** | 1.3~1.7% (3~4개) | 238개 중 3~4개만 Master 전용 |
| **최대 1차 전달** | 39~46초 | 확정적 (ISL 홉 수에 의존) |

---

## 부록: 분석 코드

본 분석에 사용된 SGP4 기반 정량 분석 스크립트:
`analysis_master_placement.py`

실행 방법:
```bash
python analysis_master_placement.py
```

콘스텔레이션 생성 코드는 기존 `tle_generator.py`의
`generate_walker_delta_constellation()` 및 `generate_tle_from_kepler()` 함수를
그대로 사용한다.
